import React from 'react';
import {Box, Text} from 'react-native-design-utility';

const ListenNow = () => {
  return (
    <Box f={1} center>
      <Text>List Now Screen</Text>
    </Box>
  );
};

export default ListenNow;
